gosorio@DESKTOP-78GDB06:~/myapp$ cat .github/workflows/*
   name:Deploy to Render

   on:
     push:
       branches:
         - main

   jobs:
     test-and-deploy:
       runs-on: ubuntu-latest

       steps:
       - name: Validacion previa
         run: find .

       - name: Checkout code
         uses: actions/checkout@v3


       - name: Validacion previa
         run: find .

       - name: Set up Python
         uses: actions/setup-python@v4
         with:
           python-version: '3.x'

       - name: Install pytest
         run: pip install pytest

       - name: Deploy to Render
         if: success()
         env:
         run: |
           curl -X POST "https://api.render.com/deploy/srv-csnbgmtds78s7392g9p0?key=r4QYZpdSW1E" -H "Content-Type: application/json" -d '{}'
